# llvm-dev devcontainer

This is a devcontainer configuration for llvm-dev based on the settings from https://apt.llvm.org/ .
Current contained version of LLVM is 22, container is built on Ubuntu 24.04.

This installs "all" packages and also compiler-rt, mlir.
